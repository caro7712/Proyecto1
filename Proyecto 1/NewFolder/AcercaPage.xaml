<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Proyecto_1.NewFolder.AcercaPage"
             BackgroundColor="#BFACAC"
             Title="AcercaPage">
    <VerticalStackLayout>
        <Label 
            Text="AcercaPage"
            FontSize="35"
            Margin="100,30"/>
        <Label 
                Text="Trabajamos con el propósito de hacer accesibles las tendencias en alimentación para que todas las personas puedan vivir mejor.
                
            Por eso, innovamos en nuestras tres divisiones: Alimentos de consumo masivo, Agronegocios y Packaging."
                FontSize="27"
                LineHeight="1" 
                BackgroundColor="#D9D9D9"
                Margin="30,0,30,0"/>
        <Image Margin="0,10" Source="unnamed.png" WidthRequest="260" />
        <Frame 
            Padding="0" 
            HasShadow="True" 
            CornerRadius="10" 
            BackgroundColor="#D9D9D9"
            Margin="0,25">

            <Grid>
                <!-- Botón funcional que cubre todo -->
                <Button 
                    BackgroundColor="Transparent"
                    Clicked="btnMenu_Clicked" />

                <!-- Contenido encima del botón -->
                <HorizontalStackLayout 
                    VerticalOptions="Center" 
                    HorizontalOptions="Center"
                    Spacing="10">

                    <ImageButton 
                        Source="menu.png" 
                        WidthRequest="60" 
                        HeightRequest="60" 
                        BackgroundColor="Transparent"
                        IsEnabled="False"/>
                    <!-- Desactivado para que el click pase al Button -->

                    <Label 
                        Text="Menu" 
                        FontSize="40" 
                        VerticalOptions="Center"
                        TextColor="Black"/>
                </HorizontalStackLayout>
            </Grid>
        </Frame>

    </VerticalStackLayout>

    
</ContentPage>