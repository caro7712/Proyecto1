<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Proyecto_1.NewFolder.Menu"
             BackgroundColor="#BFACAC"
             Title="Menu">

    <VerticalStackLayout>
        <Label Text="Menu" />
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Margin="0,5" BackgroundColor="#D9D9D9" Text="Acerca" Clicked="btnAcerca_Clicked"/>
            <Button Grid.Column="1" Margin="0,5" BackgroundColor="#D9D9D9" Text="Home" Clicked="btnHome_Clicked"/>
            <Button Grid.Column="2" Margin="0,5" BackgroundColor="#D9D9D9" Text="Salir" Clicked="Salir_Clicked"/>
            
        </Grid>
    </VerticalStackLayout>

</ContentPage>
