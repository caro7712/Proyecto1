<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Proyecto_1.NewFolder.Menu"
             BackgroundColor="#BFACAC"
             Title="Menu">

    <VerticalStackLayout>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Row="0" Grid.Column="0" BackgroundColor="#BFACAC" Text="Acerca" Clicked="btnAcerca_Clicked"/>
            <Button Grid.Row="0" Grid.Column="1" BackgroundColor="#BFACAC" Text="Desarrollador" Clicked="btnDesarrollador_Clicked"/>
            <Button Grid.Row="1" Grid.Column="1" BackgroundColor="#BFACAC" Text="Salir" Clicked="Salir_Clicked"/>
            
        </Grid>
    </VerticalStackLayout>

</ContentPage>
